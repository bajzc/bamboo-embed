# Python / uv
.venv/
__pycache__/
*.pyc
.pytest_cache/
*.egg-info/

# direnv
.direnv/

# secrets — never commit API keys
.env
.env.*
!.env.example

# Data artifacts (regenerable; raw corpus is a large read-only clone)
data/raw/
data/lancedb/
data/*.sqlite
data/passages.jsonl
data/char_report.json
# manifest.json and pua_map.json are small + hand-editable -> keep tracked

.vscode

# GGUF model weights (regenerable via download; multi-GB)
models/

# guji release pack output (multi-GB tarballs; published to HF, not committed)
dist/
