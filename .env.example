# Copy to .env and fill in. .env is gitignored — never commit real keys.
# DashScope (阿里云百炼) key: used for cloud LLM (qwen-max, HyDE) and rerank (qwen3-rerank).
DASHSCOPE_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
